name: Build iOS IPA

run-name: "ios / ${{ inputs.candidate_id || github.event.pull_request.number }}"

on:
  pull_request:
    branches: [main, dev]
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - 'LICENSE*'
  workflow_dispatch:
    inputs:
      candidate_id:
        description: 'Registered candidate, for example 1.0.1+2'
        required: true
        type: string

permissions:
  contents: read
  actions: read


jobs:
  build-ios:
    # macos-13 is no longer available on GitHub-hosted runners. macos-14
    # provides Xcode 15.4, which supports this project's iOS 12+ target.
    runs-on: macos-14
    env:
      GH_TOKEN: ${{ github.token }}
      CANDIDATE_ID: ${{ inputs.candidate_id }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Resolve registered candidate
        if: github.event_name == 'workflow_dispatch'
        id: candidate
        run: python -m scripts.versioning.build resolve --candidate "$CANDIDATE_ID"

      - name: Checkout exact candidate
        if: github.event_name == 'workflow_dispatch'
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.candidate.outputs.build_sha }}
          fetch-depth: 0

      - name: Verify candidate source and version
        if: github.event_name == 'workflow_dispatch'
        run: python -m scripts.versioning.build verify --candidate "$CANDIDATE_ID"

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '15.4'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.0'
          channel: 'stable'
          cache: true

      - name: Flutter doctor
        run: flutter doctor -v

      - name: Get dependencies
        run: flutter pub get

      - name: Generate drift code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Verify version rules
        run: python -m unittest discover -s scripts/tests -v

      - name: Analyze against reviewed baseline
        run: python -m scripts.versioning.analyze

      - name: Flutter tests
        run: flutter test --no-pub --reporter expanded

      - name: Build iOS (no codesign)
        if: github.event_name == 'workflow_dispatch'
        run: |
          flutter build ios --release --no-codesign --config-only
          xcodebuild -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -sdk iphoneos \
            -destination generic/platform=iOS \
            -derivedDataPath build/ios/DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            ONLY_ACTIVE_ARCH=NO

      - name: Create unsigned IPA
        if: github.event_name == 'workflow_dispatch'
        run: |
          APP_PATH="build/ios/DerivedData/Build/Products/Release-iphoneos/Runner.app"
          mkdir -p build/ios/ipa/Payload
          cp -r "$APP_PATH" build/ios/ipa/Payload/
          cd build/ios/ipa
          zip -r OViewer.ipa Payload
          rm -rf Payload

      - name: Inspect package and write build metadata
        if: github.event_name == 'workflow_dispatch'
        env:
          ANDROID_SIGNING_CERT_SHA256: ${{ vars.ANDROID_SIGNING_CERT_SHA256 }}
        run: python -m scripts.versioning.build metadata --candidate "$CANDIDATE_ID" --platform ios --file build/ios/ipa/OViewer.ipa

      - name: Upload IPA artifact
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          path: build/ios/ipa/OViewer.ipa
          # Upload the single installer directly instead of wrapping it in a ZIP.
          archive: false
          overwrite: true
          if-no-files-found: error
          retention-days: 30

      - name: Upload build metadata
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: build-metadata
          overwrite: true
          path: build-metadata.json
          if-no-files-found: error
          retention-days: 30
