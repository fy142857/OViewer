# 版本管理实施验收记录

## 已完成

- Flutter 全量回归：142 项通过（含版本显示的加载中、读取失败、中英文提示、实际版本和构建号）。
- 版本与发布脚本回归：35 项通过，覆盖版本规则、台账并发与中断恢复、包校验、失败保护及草稿恢复。
- 本地 iOS 工具回归：76 项通过；工具及其测试继续保持 Git 忽略。另使用真实产物完成 IPA 识别、下载、命名、内部版本及候选元数据校验。
- 静态分析：已有 6 条 INFO，没有新增错误或警告；Android、iOS CI 也通过基线检查。
- 首个候选 `1.0.1+2`：源提交 `6aef794a541cc1077962c927f8da631df6287918`，最终构建提交 `e5b72976cbd98949c0e165995fe003eeef19b6e6`。
- 首轮构建成功：Android run `35930392785`，iOS run `35930395607`。下载后的安装包内部版本、提交登记、大小、SHA-256 与元数据一致。
- 原候选准备 run `35930365892` 第 2 次运行成功：台账仍只有 1 个候选、最大构建号仍为 2，没有重复启动两平台构建。
- 发布端 artifact 下载的请求头已通过真实下载验证；存储跳转不会转发 GitHub 凭据。
- 后续两次有效修改依次生成 `1.0.1+3`、`1.0.1+4`，每个候选均只有一份公共版本登记。两平台各候选构建均成功。
- 第一轮验证候选 `1.0.1+4` 构建提交：`ccf407806f9932d427dc53e581efad4bc79a36e4`。已下载并核验两个安装包，包内版本、大小、SHA-256 均匹配元数据。
- Android `+2`、`+3`、`+4` 的正式证书相同。
- `dev → main` 快进晋升后，准备运行 `35936603229` 成功，仍为 `1.0.1+4`；日志明确复用 Android run `35935908595` 与 iOS run `35935910886`，没有重新编译或分配编号。
- 发布只校验运行 [35936640334](https://github.com/fy142857/OViewer/actions/runs/35936640334) 成功，输入为 `v1.0.1`、上述两次最终构建及 `check_only=true`。另用真实 `+2` 产物确认：未包含在 `main` 中时会拒绝发布。
- [v1.0.1 Milestone](https://github.com/fy142857/OViewer/milestone/1) 已建立，保持开启；现有 Issue 编号和完成状态未改动。
- 已核对仓库仍只有原有 `v1.0.0` Release，其两个附件 ID 和 SHA-256 保持不变；没有创建 `v1.0.1` 标签或 Release。

## 最终候选产物

按用户要求补充签名备份忽略规则后，自动生成最终候选 **`1.0.1+5`**，构建提交为 `ffe3ebfaa178a807855240430caf4ca5b21ca179`。两平台构建成功，已下载并核验实际安装包的内部版本、文件大小、SHA-256 及 Android 正式证书。

晋升 `main` 的准备运行 `35974362480` 成功，台账仍为 4 个候选、最大构建号 5。最终发布只校验运行 [35974374765](https://github.com/fy142857/OViewer/actions/runs/35974374765) 成功，未创建新标签或 Release。

| 平台 | 构建 run ID | SHA-256 |
|---|---|---|
| Android | [35972643018](https://github.com/fy142857/OViewer/actions/runs/35972643018) | `b6798d41f75550bbb484f5fca2c1848acabcfb65b836010a1c147cda566a52f8` |
| iOS | [35972645772](https://github.com/fy142857/OViewer/actions/runs/35972645772) | `1710877f87d6a652f294d8fd995a9817aae0bfa82c4ad8469ae7beab72290c9e` |

## 签名

正式证书 SHA-256：

```text
a30360a24fda76486fc152cb9e2e8cc5e7578c42c46d6aa430f3ae35adfa34e4
```

实际核验 v1.0.0 APK：原包使用 Android Debug 证书，SHA-256 为：

```text
a447d18c3eb7db00cbac4418ef505facf5d2d408ee64311817050ea25977a1e2
```

两者不同。新正式包无法直接覆盖 v1.0.0，需卸载后重装，可能丢失本地历史、进度和设置。后续正式包固定使用新证书。

四项签名 Secrets 和证书指纹变量已配置。密钥原件保存在仓库外。按用户指定位置，另备份到本地 `.release/signing_key/`：5 个文件已逐一校验与原件完全一致，目录仅授予当前用户及 SYSTEM 访问权限；该目录已加入 Git 忽略规则，没有文件被跟踪。

## Android 真机验收

- 设备：VOG-AL10，Android 10。
- 原安装包的 SHA-256 与 v1.0.0 Release 完全一致。保留数据覆盖安装 `1.0.1+2` 时，系统返回 `INSTALL_FAILED_UPDATE_INCOMPATIBLE`，明确报告签名不匹配；旧版仍为 `1.0.0 (1)`，安装/更新时间不变。
- 经用户明确授权，卸载该设备旧版后安装新签名 `1.0.1+2` 成功。设置页实际显示 `1.0.1 (2)`。
- 在正式候选中，通过正常 UI 设置英文、深色主题，并浏览本机临时服务提供的合成测试画廊 `OViewer Upgrade QA`，阅读到第 `7 / 20` 页。未改动安装包或私有数据库，也未用调试包替代正式候选。
- 使用同一正式证书签名的原始 `1.0.1+5` APK 执行 `adb install -r`，覆盖升级成功；UID 和首次安装时间保持不变。
- 升级后的历史列表仍包含该画廊及 `7 / 20` 进度；重新进入阅读器实际恢复到第 7 页。英文和深色主题保留，设置页显示 `1.0.1 (5)`，与 APK、源码和元数据一致。
- 验收完成后，仅删除合成测试历史，将测试期间更改的语言及主题恢复为中文、跟随系统。已清除应用内临时代理、USB 端口转发和设备临时 UI 文件，并停止本地测试服务；手机保留 `1.0.1 (5)`。

## 验证范围

本次真机验证覆盖上述 Android 10 设备，不代表所有 Android 型号均已实测。本地 iOS 工具的自动回归与真实下载已通过，但本次未连接 USB iPad，未进行真实签名及安装。Windows 本地 Gradle 前置任务校验因无法建立 loopback 连接而未执行到签名检查；正式签名包构建及包内证书核验已在 GitHub Linux runner 成功完成。

草稿恢复、附件冲突、重复发布等会写入 GitHub 的场景已用隔离测试覆盖；本次不以创建公开 Release 的方式测试，也不修改 v1.0.0。
